# Docker Hub username for pulling published images (used by docker-compose)
# Create a Docker Hub access token at https://hub.docker.com/settings/security
# Then add DOCKERHUB_USERNAME and DOCKERHUB_TOKEN as GitHub repo secrets
DOCKERHUB_USERNAME=sahil4818
